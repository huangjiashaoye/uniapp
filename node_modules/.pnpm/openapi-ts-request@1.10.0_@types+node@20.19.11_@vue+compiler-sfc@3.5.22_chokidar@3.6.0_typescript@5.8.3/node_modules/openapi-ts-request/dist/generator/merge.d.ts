import { type MergeOption, type MergerOptions } from './type';
export declare class Merger {
    #private;
    constructor({ srcPath, source, mergeRule, projectOptions, }: MergerOptions);
    merge({ srcPath, source }: MergeOption): string;
}
