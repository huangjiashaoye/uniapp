import { e as ComponentResolver } from './types-be8c3f97.js';
import { FilterPattern } from '@rollup/pluginutils';
import '@antfu/utils';

declare function AnoResolver(): ComponentResolver;

interface UniUIResolverOptions {
    /**
     * RegExp or string to match component names that will NOT be imported
     */
    exclude?: FilterPattern;
}
declare function UniUIResolver(options?: UniUIResolverOptions): ComponentResolver;

declare function WotResolver(): ComponentResolver;

declare function UvResolver(): ComponentResolver;

/**
 * uView Pro 组件解析器
 * @param prefix - 组件导入路径的前缀，默认为 'uview-pro'
 * @param [prefix='uview-pro'] - 可传入 uni_modules 路径以支持 uni_modules
 * @returns uView Pro 组件的解析器函数
 * @example
 * // 基本用法
 * uViewProResolver()
 *
 * // 支持 uni_modules
 * uViewProResolver('@/uni_modules/uview-pro')
 */
declare function uViewProResolver(prefix?: string): ComponentResolver;

export { AnoResolver, UniUIResolver, UniUIResolverOptions, UvResolver, WotResolver, uViewProResolver };
